import { styled } from '@mui/material';

export const StyledRegistrationInfoDiv = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',

  '& img': {
    height: '20rem',
    margin: '0 auto',
    display: 'block',
  },
});
